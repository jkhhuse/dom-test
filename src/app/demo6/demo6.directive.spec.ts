import { Demo6Directive } from './demo6.directive';

describe('Demo6Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo6Directive();
    expect(directive).toBeTruthy();
  });
});

import { Demo3Directive } from './demo3.directive';

describe('Demo3Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo3Directive();
    expect(directive).toBeTruthy();
  });
});

import { Demo5Directive } from './demo5.directive';

describe('Demo5Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo5Directive();
    expect(directive).toBeTruthy();
  });
});

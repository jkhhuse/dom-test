import { Demo4Directive } from './demo4.directive';

describe('Demo4Directive', () => {
  it('should create an instance', () => {
    const directive = new Demo4Directive();
    expect(directive).toBeTruthy();
  });
});
